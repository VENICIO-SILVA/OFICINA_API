spring.application.name=oficina

server.port=8080

# Configuração do banco de dados (exemplo com MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/oficina?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=coxinha220

# Driver JDBC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update   
spring.jpa.show-sql=true               
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=America/Sao_Paulo
spring.jackson.time-zone=America/Sao_Paulo

# Configuração de logs
logging.level.org.springframework=INFO
logging.level.com.vini.oficina=DEBUG

